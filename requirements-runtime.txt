# Runtime dependencies for packaged installs.
pydantic>=2.0,<3.0
cryptography>=50.0.0
pynacl>=1.5
keyring>=24.0  # OS-keystore key protection (Windows DPAPI / macOS Keychain / Linux Secret Service)
psutil>=5.9
pyyaml>=6.0
pypdf==6.19.0  # Projection of the confined PDF fallback in pyproject.toml.
reportlab==5.0.1
markdown-it-py==4.2.0
xlrd==2.0.1  # Projection of the legacy XLS reader in pyproject.toml.
requests>=2.28
starlette>=1.3.1,<2.0
anyio>=4.14.2
uvicorn>=0.30,<1.0
openai>=1.0
anthropic>=0.18
sentence-transformers>=2.2
torch>=2.13.0
transformers>=4.48
peft>=0.14
accelerate>=1.2
aiohttp>=3.14.3
datasets>=3.2
huggingface_hub>=0.28
playwright>=1.52,<2.0
zstandard>=0.22.0
xxhash>=3.4.0
pytest>=7.0
# Solana: required at RUNTIME, not just for live payments. The .null resolver needs
# solders' find_program_address (the hand-rolled fallback derives wrong PDAs for some
# names, breaking web0.null resolution), and the agent wallet's x402 signing path needs
# it too. Without this the whole .null/x402/Web0 layer silently degrades in installs.
solders>=0.21
# Apple Calendar (EventKit) native adapter: the pyobjc binding the packaged host needs on
# macOS. Without it the eventkit adapter reports its typed os_binding_unavailable state and
# Apple Calendar stays honestly unsupported; with it the adapter runs against the real store.
pyobjc-framework-EventKit>=12.2.2; sys_platform == "darwin"
