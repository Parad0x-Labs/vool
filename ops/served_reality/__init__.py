"""ops.served_reality — the exact-SHA served-reality bench.

A release-critical harness that proves the ASSEMBLED product — the real
daemon, served over HTTP, from an immutable staging of one exact commit —
rather than isolated modules. See README.md for usage.
"""

__all__ = ["SCHEMA_VERSION"]
SCHEMA_VERSION = "1.0.0"
