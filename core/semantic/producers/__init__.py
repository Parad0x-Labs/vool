"""Producers build a ``RequestGraph`` from a turn. The lexical producer is the deterministic reading
the runtime acts on today, with stable ids; the model resolver (``core.semantic.resolver``) is the
reading compared against it in SHADOW."""
