# Core
pydantic>=2.0,<3.0
cryptography>=41.0
pynacl>=1.5
keyring>=24.0  # OS-keystore key protection (Windows DPAPI / macOS Keychain / Linux Secret Service)
psutil>=5.9
pyyaml>=6.0
pypdf==6.18.1  # Projection of the confined PDF fallback in pyproject.toml.
reportlab==5.0.1
markdown-it-py==4.2.0
xlrd==2.0.1  # Projection of the legacy XLS reader in pyproject.toml.

# HTTP / Networking
requests>=2.28
starlette>=0.37,<2.0
uvicorn>=0.30,<1.0

# Solana x402 settlement (live devnet/mainnet pay path; the default offline mode needs none of this)
solders>=0.21
# EVM x402 v2 settlement (EIP-712 digests, EIP-3009 recovery): imported lazily by
# core.wallet.evm, so the default offline/disabled mode needs none of this
# EVM x402 v2 lane (optional extra `evm` in pyproject). core.wallet.evm resolves
# these at USE and returns a typed wallet_dependency_unavailable when absent, so
# Solana, custody, ceilings and the mainnet guard do not need them.
eth-abi>=6.0
eth-utils>=6.0
eth-account>=0.13

# AI / Model backends
openai>=1.0
anthropic>=0.18
sentence-transformers>=2.2
torch>=2.5
transformers>=4.48
peft>=0.14
accelerate>=1.2
datasets>=3.2
huggingface_hub>=0.28
playwright>=1.52,<2.0

# Liquefy companion deps (liquefy-openclaw itself is cloned+patched by the installer)
zstandard>=0.22.0
xxhash>=3.4.0

# Development
pytest>=7.0
ruff>=0.3
